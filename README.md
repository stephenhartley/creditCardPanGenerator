# creditCardPanGenerator
Java source to generate potentially valid credit card numbers (PAN) given a BIC (real or invalid).
Numbers returned pass Luhn modulus check.
Useful to generate test datasets.
